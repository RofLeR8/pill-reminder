import { useState } from 'react';
import { HeartPulse } from 'lucide-react';
import { Link, useNavigate } from 'react-router-dom';
import { Button } from '../components/Button/Button';
import './Pages.css';
export const AuthPage = ({ mode }: { mode: 'login' | 'register' }) => { const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const navigate=useNavigate(); const register=mode==='register'; const submit=(e:React.FormEvent)=>{e.preventDefault(); localStorage.setItem('pill-reminder-auth','true'); if(name) localStorage.setItem('pill-reminder-name',name); navigate('/');}; return <main className="auth-page"><section className="auth-card"><div className="auth-logo"><HeartPulse size={30}/></div><p className="page__eyebrow">Таблетница</p><h1>{register ? 'Создать аккаунт' : 'С возвращением'}</h1><p>{register ? 'Зарегистрируйтесь, чтобы начать вести расписание.' : 'Войдите в демо-аккаунт для доступа к приложению.'}</p><form onSubmit={submit}>{register && <label>Имя<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Как к вам обращаться"/></label>}<label>E-mail<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Пароль<input required type="password" minLength={4} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Не менее 4 символов"/></label><Button type="submit" fullWidth size="lg">{register ? 'Зарегистрироваться' : 'Войти'}</Button></form><p className="auth-switch">{register ? 'Уже есть аккаунт?' : 'Впервые здесь?'} <Link to={register ? '/login' : '/register'}>{register ? 'Войти' : 'Создать аккаунт'}</Link></p></section></main> };
